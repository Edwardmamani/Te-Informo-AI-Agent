services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: te-informo-backend
    ports:
      - "3001:3001"
    volumes:
      # Montar código fuente para hot-reload
      - ./backend/src:/app/src
      - ./backend/package.json:/app/package.json
      - ./backend/tsconfig.json:/app/tsconfig.json
      - ./backend/nodemon.json:/app/nodemon.json
      # Excluir node_modules del volumen (usar los del contenedor)
      - /app/node_modules
      - /app/dist
    env_file:
      - backend/.env
    environment:
      - NODE_ENV=development
      - PORT=3001
      # Habilitar polling para detectar cambios en archivos
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      # GEMINI_API_KEY se cargará desde backend/.env
    restart: unless-stopped
    networks:
      - te-informo-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: te-informo-frontend
    ports:
      - "3000:3000"
    volumes:
      # Montar código fuente para hot-reload
      - ./frontend:/app
      # Excluir node_modules del volumen
      - /app/node_modules
    environment:
      - NODE_ENV=development
      # Habilitar polling para detectar cambios en archivos
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - ELECTRON_DISABLE_SECURITY_WARNINGS=true
    restart: unless-stopped
    networks:
      - te-informo-network
    # Depende del backend
    depends_on:
      - backend

networks:
  te-informo-network:
    driver: bridge

